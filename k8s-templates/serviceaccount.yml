# identity for pods to talk to the K8s API
apiVersion: v1
kind: ServiceAccount
metadata:
  name: my-service-account
  namespace: default
  # for AWS IRSA — uncomment and set your role ARN
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/my-role
